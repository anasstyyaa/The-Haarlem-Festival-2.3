version: '3'

services:
  nginx:
    image: nginx:latest
    ports:
      - "8081:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./app:/app
    depends_on:
      - php

  php:
    build:
      context: .
      dockerfile: PHP.Dockerfile
    volumes:
      - ./app:/app
    environment:
      DB_SERVER: "somerendbv2.database.windows.net"
      DB_PORT: "1433"
      DB_NAME: "anastasia"
      DB_USER: "frankde"
      DB_PASS: "__ka3tana2@@"

volumes: {}